// @flow

// import { modelizerWrapper } from 'shared-components-molecules/fund/utils';

// const modelizer: WidgetTemplateModelizerType = apiResponse => ({
//   id: apiResponse.fundshare_id,
// });

// export default modelizerWrapper(modelizer);
